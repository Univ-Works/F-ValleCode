# Clase Alumno
- - -
Crea una clase llamada **Alumno** con los siguientes atributos: `nombres`, `apellidos` y `dni`.

Ejemplo:

```Java
class ValleCode {
    String attibute1;
    String attibute2;
}
```